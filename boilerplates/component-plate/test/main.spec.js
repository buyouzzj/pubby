import { should, expect } from 'chai';

describe('ComponentTest', () => {
  'use strict';

  before(() => {
  });

  it('should be ok', function (done) {
    expect('ok').to.be.a('string');
    done();
  });
});
